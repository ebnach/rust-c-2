# write Makefile for Rust project with format, lint, test, build, run, clean, and doc targets

format:
	cargo fmt

lint:
	cargo clippy

test:
	cargo test

build:
	cargo build

run:
	cargo run

clean:
	cargo clean

doc:
	cargo doc --no-deps --open

.PHONY: format lint test build run clean doc